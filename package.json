{
  "name": "m2a",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev:web": "npm run --prefix apps/web dev",
    "dev:api": "npm run --prefix apps/api start:dev"
  }
}
